import { createTheme } from '@mui/material/styles';


const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#BF00FF'
        },
        secondary: {
            main: '#40FF00'
        }
    }
})

export default theme;